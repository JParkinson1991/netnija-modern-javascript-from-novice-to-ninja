// logical NOT (!)

console.log(!true);
console.log(!false);

// False indicates not logged in
let user = false;

// When user not logged in
if (!user) {
    console.log('you must be logged in to continue')
}